from setuptools import setup

setup(name = 'rocrateValidator',
	version = '0.0.1', 
	description = 'This is python library for ro-crate validator',
	py_modules = ['ro-crate-validator-py'],
	zip_safe = False
	)
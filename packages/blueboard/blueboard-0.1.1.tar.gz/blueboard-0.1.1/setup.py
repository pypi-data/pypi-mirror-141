from setuptools import setup

setup(name='blueboard',
      version='0.1.1',
      description='Python Package',
      packages = ['package'],
      install_requires = [],
      zip_safe = False,
      )
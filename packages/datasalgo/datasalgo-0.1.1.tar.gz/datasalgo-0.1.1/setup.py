from setuptools import setup

setup(name='datasalgo',
      version='0.1.1',
      description='Data Structures and Algorithms package Package',
      packages = ['datasalgo'],
      install_requires = ["utilum"],
      zip_safe = False,
      )
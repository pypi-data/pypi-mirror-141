[tool.poetry]
name = "gamma-cli"
version = "0.0.0"
description = ""
authors = ["Niels Freier <freier.niels@bcg.com>"]

[tool.poetry.dependencies]
python = "^2.7"

[tool.poetry.dev-dependencies]
pytest = "^4.6"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
